# additional index (for work)
[[index]]
name = "ks"
url = "http://dev.ks.int/store/repository/pypi/simple"
publish-url = "http://dev.ks.int/store/repository/pypi/simple"
explicit = true

# always use system Python interpreter - do not install managed interpreters –
# at least for now...
python-preference = "only-system"
